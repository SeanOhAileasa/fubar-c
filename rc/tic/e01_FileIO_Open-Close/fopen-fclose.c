/* a file is simply a named portion of storage in the same manner as labelling a sequential set of memory locations and calling it an array */
#include <stdio.h>
#include "openfile.h"
int main(int argc, char *argv[])
{
    openFile();
    return 0;
}
